{
  "name": "socket-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "mock:hardware": "bun scripts/mock-hardware.ts",
    "mock:hardware:1": "bun scripts/mock-hardware.ts raspberry-pi-1",
    "mock:hardware:2": "bun scripts/mock-hardware.ts raspberry-pi-2",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "socket.io-client": "^4.8.3",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "socket.io": "^4.8.3",
    "zod": "^4.4.3"
  }
}